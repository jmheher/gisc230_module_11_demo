# Demo 3

This demo extends the mobile-aware web map into a baseline Progressive Web App. It includes a manifest and a service worker that caches the local application shell files.

Run this demo from a local server so the service worker can register correctly.
